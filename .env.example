# Environment Configuration
# Copy this file to .env.local and update values as needed

# Target environment for edge API calls
# Options: qa | st | prod
# Default: qa
VITE_ENVIRONMENT=qa

# SDK Version (auto-populated during build)
VITE_SECURE_SDK_VERSION=0.0.1
